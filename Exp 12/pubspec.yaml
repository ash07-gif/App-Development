name: flutter_firebase_auth_demo
description: Flutter Firebase auth demo
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.12.0
  firebase_auth: ^4.6.0
  google_sign_in: ^6.1.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
